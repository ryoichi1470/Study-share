<h2>コメントを編集</h2>
<%= form_with(model: [ @post, @comment ]) do |form| %>
  <div>
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>
  <%= form.submit '更新' %>
<% end %>
<%= link_to 'キャンセル', post_path(@post) %>